{
  "name": "control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "Dashboard local para orquestar varias sesiones de Claude Code sobre un mismo repo.",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,web -c blue,magenta \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run build && npm run start -w server",
    "serve": "npm run start -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "test": "npm test -w server"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  },
  "engines": {
    "node": ">=24"
  }
}
